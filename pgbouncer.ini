[databases]
* = host=127.0.0.1 port=5432 auth_user=postgres

[pgbouncer]
listen_port = 6432
listen_addr = 0.0.0.0
pool_mode = session

default_pool_size=99
min_pool_size=99

auth_type = trust
admin_users = postgres
stats_users = postgres
